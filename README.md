Hadoop-YARN-Dynamic-container
=============================

This is a project to implement elastic container usage on hadoop yarn for long running jobs, so that they can make better use the cluster resources.
